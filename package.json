{
  "name": "deployment-actions",
  "version": "1.0.0",
  "description": "GitHub Action for managing deployments",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build lib/index.js -o dist && ncc build lib/post.js -o dist",
    "compile": "tsc",
    "package": "ncc build lib/index.js -o dist && ncc build lib/post.js -o dist-post",
    "all": "npm run compile && npm run package"
  },
  "keywords": ["github", "actions", "deployment"],
  "author": "JD Boivin",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}